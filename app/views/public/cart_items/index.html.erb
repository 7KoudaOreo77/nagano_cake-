<h1>ショッピングカートPublic::CartItems#index</h1>
<p>Find me in app/views/public/cart_items/index.html.erb</p>

<% @cart_items.each do |cart_item| %>
 <p>商品名</p><%= cart_item.item.name %>
 <p>単価(税込)</p><%= cart_item.item.with_tax_price %>
 <p>数量</p><%= cart_item.amount %>
 <p>小計</p><%= cart_item.subtotal %>

<% end %>

<table>
  <thead>
      <th>単価(税込)</th>
      <th>小計</th>
  </thead>
  <tbody>
  <% @cart_items.each do |cart_item| %>
   <tr>
    <td><%= cart_item.item.with_tax_price %></td>
    <td><%= cart_item.subtotal %></td>
    <%# @cart_item_price +=  cart_item.subtotal %>
   </tr>
  <% end %>
  </tbody>
</table>

<p>合計金額</p>

<%= link_to "買い物を続ける", public_items_path %>
<%= link_to "情報入力に進む", new_public_order_path %>